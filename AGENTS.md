# Agents

## Session Lessons

- `JavaScript: 乐观更新 UI 后才发送请求，发送时用被更新的变量而非原值 — 解决：在修改任何全局状态前先 capture 原始值（如 `const originalName = currentFile`），再执行乐观更新和发送请求`
- `K8s 部署：make build 构建推送镜像，kubectl rollout restart deploy <name> 滚动重启拉取新镜像；无需修改 yaml 文件，镜像 tag 不变时重启即可更新`
